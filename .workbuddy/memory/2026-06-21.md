## 2026-06-21 01:47

### 修复背景色未撑满屏幕（三轮修复）
- ...（略，见上方）
### 合并 ApiTester → App.vue
- ...（略，见上方）
### 修复 @import 顺序错误
- Tailwind v4 处理后 @import 会被插入生成的 CSS 中间导致 `@import must precede all other statements` 报错
- 将 Google Fonts 的 `@import url(...)` 从 style.css 改为 index.html 中的 `<link>` 标签
### 修复下拉框与按钮重叠
- ...（略，见上方）
### 添加协议切换选择器
- api-test.ts: `testApiKey` 新增可选 `protocolOverride` 参数，覆盖厂商默认协议
- App.vue: 新增协议下拉框（OpenAI/Anthropic/Gemini），切换厂商时自动跟随默认协议，可手动覆盖
- 位置：厂商选择器和 Base URL 之间
### 开源文档 & GitHub 发布
- 编写中文 README.md：项目介绍、特性、厂商表、快速开始、技术栈、隐私说明、使用方式、自定义模式
- 添加 MIT LICENSE
- 通过 `gh` CLI 创建公开仓库并推送: https://github.com/wlxweb/ai-key-tester

